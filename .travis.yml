sudo: required

language: perl

services: docker

install: true

script:
- docker pull analizo/test
- docker create --name travis -v `pwd`:/analizo analizo/test
- docker start travis
- docker logs -f travis
- exit $?
